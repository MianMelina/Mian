Private Sub Verwaltung_Initialize()
    ' Initialisierung der UserForm ohne Verzögerung
    Me.Caption = ""
    
    ' Status des ToggleButtons laden
    LoadToggleButtonStateVerwaltung
    
    ' Überprüfen, ob der ToggleButton aktiviert ist
    If Not Me.ToggleButton1.Value Then
        ' Timer starten, um die PDF-Suche nach dem Öffnen zu beginnen
        Application.OnTime Now + TimeValue("00:00:01"), "StartPdfSearchVerwaltung"
    End If
End Sub

Private Sub Verwaltung_QueryClose(Cancel As Integer, CloseMode As Integer)
    ' Status des ToggleButtons speichern, wenn die UserForm geschlossen wird
    SaveToggleButtonStateVerwaltung
End Sub

Private Sub ToggleButton1_Click()
    ' Wenn der ToggleButton geändert wird, den Status speichern
    SaveToggleButtonStateVerwaltung
    
    ' Überprüfen, ob der ToggleButton aktiviert oder deaktiviert wurde
    If Me.ToggleButton1.Value Then
        ' PDF-Suche deaktiviert
    Else
        ' PDF-Suche aktiviert
        StartPdfSearchVerwaltung
    End If
End Sub

Sub StartPdfSearchVerwaltung()
    StartPdfSearch "Verwaltung"
End Sub






Private Sub Dispoerstellen_Initialize()
    ' Initialisierung der UserForm ohne Verzögerung
    Me.Caption = ""
    
    ' Status des ToggleButtons laden
    LoadToggleButtonStateDispo
    
    ' Überprüfen, ob der ToggleButton aktiviert ist
    If Not Me.ToggleButton1.Value Then
        ' Timer starten, um die PDF-Suche nach dem Öffnen zu beginnen
        Application.OnTime Now + TimeValue("00:00:01"), "StartPdfSearchDispo"
    End If
End Sub

Private Sub Dispoerstellen_QueryClose(Cancel As Integer, CloseMode As Integer)
    ' Status des ToggleButtons speichern, wenn die UserForm geschlossen wird
    SaveToggleButtonStateDispo
End Sub

Private Sub ToggleButton1_Click()
    ' Wenn der ToggleButton geändert wird, den Status speichern
    SaveToggleButtonStateDispo
    
    ' Überprüfen, ob der ToggleButton aktiviert oder deaktiviert wurde
    If Me.ToggleButton1.Value Then
        ' PDF-Suche deaktiviert
    Else
        ' PDF-Suche aktiviert
        StartPdfSearchDispo
    End If
End Sub

Sub StartPdfSearchDispo()
    StartPdfSearch "Dispoerstellen"
End Sub

